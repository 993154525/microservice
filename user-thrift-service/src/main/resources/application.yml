server:
  name: localhost
  port: 7911

#数据源的配置
spring:
  datasource:
    url: jdbc:mysql://${mysql.address}/microservice?useUnicode=true&characterEncoding=utf8&useSSL=false&autoReconnect=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456

